import Express from "express";
import {  } from "../controllers/comment.js"

const router = Express.Router()

router.get("", )    //when we call this endpoint "/", it calls the function which grabs info from the db

export default router